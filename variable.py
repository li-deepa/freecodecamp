# varaiables save information in its address so it is easy to reference the value using 
# the name of variable
#naming coventions for variables
# varaiables start with _ or chearacter or uppercase and 
# no space between varaiables and 
# we can concatenate the variables
#variables should never start with numbers
# be careful while using LOWERCASE l and UPPERCASE O 
# which are similar to 1 and 0

name="ram"
print(name+" is a good boy")

first_name="ravan"
print(first_name)
first_name="ram"
print(first_name)
#if we store the same variable with different values it stores the 
# last value